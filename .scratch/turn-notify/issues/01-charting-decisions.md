# 01 · charting 决议（目的地与产品语义）

Type: grilling
Status: resolved

## Question

charting 会话 grilling 拍定的目的地形态与核心产品语义（作为后续所有票的前提）。

## Answer

charting 会话（2026-09-13）与作者逐题拍定：

1. **目的地**：把实现也纳入地图——规格敲定后地图内继续发实现票，`feat/turn-notify` 分支 → 实现 → 验证 → PR 合入 main 才收工（覆盖 wayfinder 默认 plan-don't-do）。
2. **触发语义**：「不在界面正在使用的状态」= 壳失去前台焦点即通知（app 非当前活跃应用）；最小化/隐藏必然失焦，自然覆盖，不做窗口可见性与会话级判定。
3. **开关位置**：web client 设置卡（往设置槽位注册，按功能模块惯例）；持久化方式待 research 票确认。不做 mac 原生菜单/托盘开关。
4. **通知内容**：只带会话名，不含回复内容摘要（隐私优先）；点击行为默认聚焦窗口，能否跳转到对应会话待原型/规格阶段结合 client 路由能力定。
5. **多会话与失败**：每轮完成各弹一条（依赖系统通知中心堆叠），不做聚合；正常与报错轮次都通知，失败文案区分（如会话名 +「处理失败」）。
